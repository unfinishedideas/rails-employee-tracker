<%= render "layouts/errors", :object => @employee %>




  <%= form_for @employee do |f| %>


  <%= collection_select(:employee, :id, @all_projects, :id, :project_name) %>

  <%= f.submit %>
  <% end %>
